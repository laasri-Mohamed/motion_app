apply plugin: 'com.android.application'

android {
    compileSdkVersion 9
    buildToolsVersion "32.0.0"

    defaultConfig {
        applicationId "com.pfasn.detective_pf"
        minSdkVersion 9
        targetSdkVersion 23
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
